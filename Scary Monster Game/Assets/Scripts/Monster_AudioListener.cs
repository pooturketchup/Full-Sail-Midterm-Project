using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Monster_AudioListener : MonoBehaviour
{
    public SoundDrop drop;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    //private void OnTriggerStay(Collider other)
    //{
    //   // float lastKnown;
    //    //check for soundDrop collider
    //   // lastKnown =  
    //    //check if other has the same tag as the player SoundDrop
    //    if (other.tag == "SoundDrop")
    //    {
    //        Debug.Log("Searching");
    //        //set target position to the sound drop location
    //        this.GetComponentInParent<MonsterBehavior>().target = GameObject.Find("SoundDrop"); //make a smaller drop to find the center of the sound drop
    //    }
    //}
}